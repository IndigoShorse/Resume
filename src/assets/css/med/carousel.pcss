
.ui-carousel {
    width: 100%;
}

.ui-carousel__header {
    width: fit-content;
    background-color: #F1F5F9;
    border-radius: 10px;
    position: relative;
    display: flex;
    gap: 0;
}

.ui-carousel__header-highlight {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    border-radius: var(--button-radius);
    border: 2px solid var(--primary-color);
    background: rgba(146, 218, 240, 0.08);
    transition: left 0.35s cubic-bezier(0.4, 0, 0.2, 1),
    width 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 0;
    pointer-events: none;
    box-sizing: border-box;
}

.ui-carousel__tab {
    position: relative;
    z-index: 1;
    flex: 0 0 auto;
    padding: 10px 20px;
    background: none;
    border: none;
    cursor: default;
    font-size: var(--font-size-default);
    font-weight: 500;
    color: var(--subtitle-color);
    transition: color 0.3s ease, font-weight 0.3s ease;
    user-select: none;
}

.ui-carousel__tab--clickable {
    cursor: pointer;
}

.ui-carousel__tab--active {
    color: var(--text-color);
    font-weight: 600;
}

.ui-carousel__tab--completed {
    color: var(--text-color);
}

.ui-carousel__viewport {
    overflow: hidden;
    width: 100%;
    /* Вертикальное центрирование слайда; с включённой навигацией
       свободное место распределяется над и под viewport, а кнопки уходят вниз */
    margin: auto 0;
}

.ui-carousel__track {
    display: flex;
    transition: transform 0.45s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: transform;
}

.ui-carousel__slide {
    display: flex;
    justify-content: center;
    flex-shrink: 0;
    box-sizing: border-box;
}

/* Кнопки «Назад» / «Далее», прижатые к низу шага */
.ui-carousel__nav {
    display: flex;
    justify-content: space-between;
    width: 100%;
    padding: 0 24px;
    box-sizing: border-box;
}
