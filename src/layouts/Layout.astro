---
import '@/assets/css/med/main.pcss'
import '@/assets/css/global.css'
import '@/assets/css/resume.css'
import { kits } from '@/data/registry'

interface Props {
  title: string
  description?: string
}

const { title, description = 'Сайт-визитка и портфолио: кастомная UI-библиотека на Vue (Nuxt 3) и React' } = Astro.props
const { pathname } = Astro.url
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <header class="site-header">
      <nav class="site-header__nav">
        <a href="/" class="site-header__logo">Портфолио</a>
        <a
          href="/"
          class:list={['site-header__link', { 'site-header__link--active': pathname === '/' || pathname.startsWith('/vue') }]}
        >
          Резюме
        </a>
        {
          kits.map((kit) => (
            <a
              href={`/ui/${kit.key}`}
              class:list={[
                'site-header__link',
                { 'site-header__link--active': pathname.startsWith(`/ui/${kit.key}`) },
              ]}
            >
              UI-библиотека {kit.title}
            </a>
          ))
        }
      </nav>
    </header>
    <main class="site-main">
      <slot />
    </main>
  </body>
</html>
