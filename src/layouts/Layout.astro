---
import '@/assets/css/med/main.pcss'
import '@/assets/css/global.css'
import '@/assets/css/resume.css'
import { kits } from '@/data/registry'
import { getDict, format, defaultLocale } from '@/i18n'

interface Props {
  title: string
  description?: string
}

const dict = getDict()
const { title, description = dict.site.description } = Astro.props
const { pathname } = Astro.url
---

<!doctype html>
<html lang={defaultLocale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <header class="site-header">
      <nav class="site-header__nav">
        <a
          href="/"
          class:list={['site-header__link', { 'site-header__link--active': pathname === '/' }]}
        >
          {dict.site.nav.resume}
        </a>
        {
          kits.map((kit) => (
            <a
              href={`/ui/${kit.key}`}
              class:list={[
                'site-header__link',
                { 'site-header__link--active': pathname.startsWith(`/ui/${kit.key}`) },
              ]}
            >
              {format(dict.site.nav.kit, { title: kit.title })}
            </a>
          ))
        }
      </nav>
    </header>
    <main class="site-main">
      <slot />
    </main>
  </body>
</html>
