{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Astro: dev-сервер",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --host",
      "cwd": "${workspaceFolder}"
    }
  ]
}
